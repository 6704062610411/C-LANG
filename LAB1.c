#include <stdio.h>

int a=20;
int b=10;
int main()
{
    if (b>a)
    {
        printf("%d geater than %d",b,a);
    }
    else
    {
        printf("%d geater than %d",a,b);
    }
    return 0;
}
